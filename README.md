# Baseball Game

A simple canvas-based baseball game. Open `index.html` in a browser.

- Press any key to pitch
- Press `SPACE` (or click) to swing
- Count tracks balls and strikes; hits show where the ball lands
